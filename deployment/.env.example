# GitHub Personal Access Token with `repo` scope on jvalin17/judgement.
# Required for community ML data sync (upload/download to GitHub releases).
# Without this, the server still collects and uses ML data locally.
JUDGEMENT_GITHUB_TOKEN=ghp_your_token_here
